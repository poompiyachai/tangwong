<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.krisorn.tangwong">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Tang Wong"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".shareprice"></activity>
        <activity android:name=".editshare" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".pool_interface" />
        <activity android:name=".StatusAlert" />
        <activity android:name=".StatusAlert2" />
        <activity android:name=".create_itemActiviity" />
        <activity android:name=".user_Question" />
        <activity android:name=".StatusCostomer" />
        <activity android:name=".user_InterfaceIntoRoom" />
        <activity android:name=".user_idSearch" />
        <activity android:name=".user_poll" />
        <activity android:name=".user_qrcode" />
        <activity android:name=".UserDashBorad" />
        <activity
            android:name=".show_data"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".user_search" />
        <activity android:name=".profile_ex" />
        <activity android:name=".AddFeatureActivity" />
        <activity android:name=".add_feature" />
        <activity android:name=".EmailPasswordActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UsersActivity" />
        <activity android:name=".own_room" />
        <activity android:name=".MainActivity" />
        <activity android:name=".registerActivity" />
        <activity android:name=".user_roomActivity" />
        <activity android:name=".addqActivity" />
        <activity android:name=".list_itemActivity" />
        <activity android:name=".item_detail" />
        <activity android:name=".Cart" />
        <activity android:name=".Status" />
        <activity android:name=".AddminListItemView" />
        <activity
            android:name=".home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme" />
        <activity android:name=".create_roomActiviity" />
        <activity android:name=".AdminDashBoradView" />

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <activity android:name=".time" />
        <activity android:name=".notification" />
        <activity android:name=".ownRoom.carlender" />
        <activity android:name=".create_event" />
        <activity android:name=".dayevent" />
        <activity android:name=".showtime" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>